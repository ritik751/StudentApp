# StudentApp
This is a WPF MVVM-based Student Management Application built using C# and .NET. The application allows users to view, add, and edit student details, including uploading a student photo. All student data is stored locally in a JSON file, so the application works without any external database.
